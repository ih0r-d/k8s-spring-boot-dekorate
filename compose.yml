services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=k8s_demo_db'
      - 'POSTGRES_PASSWORD=postgres'
      - 'POSTGRES_USER=postgres'
    ports:
      - '5432:5432'
